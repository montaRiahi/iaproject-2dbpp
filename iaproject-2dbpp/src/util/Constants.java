package util;

import java.io.File;

/**
 * Class containing needed constants that don't find their place elsewhere.
 * This is usually the case for all configuration parameters.
 */
public final class Constants {
	
	public static final File CURRENT_DIR = new File(System.getProperty("user.dir"));
	
}
